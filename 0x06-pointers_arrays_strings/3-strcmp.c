#include "main.h"
#include <string.h>

/**
 * _strcmp - a function for comparison
 * @s1: parameter 1
 * @s2: parameter 2
 * Return: interger
 */

int _strcmp(char *s1, char *s2)
{
	int compare;

	compare = strcmp(s1, s2);
	return (compare);
}
