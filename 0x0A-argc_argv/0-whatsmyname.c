#include <stdio.h>
#include <stdlib.h>
#include"main.h"

/**
 * main - program
 * @argc:number of parameters
 * @argv:array of pointers
 * Return:Always 0
 */
int main(int __attribute__((unused))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	exit(EXIT_SUCCESS);
}
